{
  "id": "greeting-workflow",
  "description": "Create a LOUD greeting for the supplied name",
  "inputSchema": {
    "type": "object",
    "properties": { "name": { "type": "string" } },
    "required": ["name"]
  },
  "outputSchema": {
    "type": "object",
    "properties": { "message": { "type": "string" } },
    "required": ["message"]
  },
  "graph": [
    { "type": "tool", "id": "greet", "toolId": "create-greeting" },
    { "type": "tool", "id": "shout", "toolId": "shout" }
  ]
}
